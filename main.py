

def classFactice(start, poids, biais):                    # entree de dim N
    def __init__(self):
        self.entree = start
        self.biais = biais
        self.vectPoids = poids
        self.n = len(self.entree)

    def neurone(self):
        return sortie                       # sortie de dim 1

    def forward(self):
        sortie = []
        for i in range(self.n):             # on continue tant que n>1
            sortie.append(neurone(self))
        self.entree = sortie
        self.n -= 1

    def backward(self):                     # sortie du neurone
        calculgradient()
        self.vectPoids = newpoids

